import { Authentication } from "components/Pages";
import { NextPage } from "next";
import React from "react";

const AuthenticationPage: NextPage = (): JSX.Element => {
  return <Authentication />;
};

export default AuthenticationPage;
